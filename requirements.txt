# Standard requirements for WhisperX (includes PyTorch)
# For x86_64 systems with CUDA support
# Install: pip install -r requirements.txt

ctranslate2>=4.5.0
faster-whisper>=1.1.1
nltk>=3.9.1

# Restrict numpy, onnxruntime, pandas, av to be compatible with Python 3.9
numpy>=2.0.2,<2.1.0; python_version <'3.13'
onnxruntime>=1.19,<1.20.0; python_version <'3.10'
pandas>=2.2.3,<2.3.0
av<16.0.0
numpy>=2.1.0,<2.3.0; python_version >='3.13'

# Updated to >=4.0.1 for torchaudio 2.x compatibility
pyannote-audio>=4.0.1

# Speechbrain from git for torchaudio 2.x compatibility
git+https://github.com/speechbrain/speechbrain.git@develop

transformers>=4.48.0

# PyTorch dependencies (for standard installations)
torch~=2.8.0
torchaudio~=2.8.0

# Triton (only on x86_64 Linux)
triton>=3.3.0; sys_platform == 'linux' and platform_machine == 'x86_64'
